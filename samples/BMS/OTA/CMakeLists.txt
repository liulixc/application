set(SOURCES_LIST
    ${CMAKE_CURRENT_SOURCE_DIR}/ota_task.c
    ${CMAKE_CURRENT_SOURCE_DIR}/wifi_connect.c
    
)


set(HEADER_LIST
    ${CMAKE_CURRENT_SOURCE_DIR}
)


set(SOURCES "${SOURCES}" ${SOURCES_LIST} PARENT_SCOPE)
set(PUBLIC_HEADER "${PUBLIC_HEADER}" ${HEADER_LIST} PARENT_SCOPE)
